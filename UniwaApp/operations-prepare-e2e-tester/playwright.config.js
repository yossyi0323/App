// my-e2e-tester/playwright.config.js (Next.js連携 - 自動起動設定)

import { defineConfig } from '@playwright/test';

export default defineConfig({
  
  // テストファイルのあるディレクトリを指定
  testDir: './tests', 
  
  use: {
    // Next.jsの標準開発ポートを指定
    baseURL: 'http://localhost:3000', 
    browserName: 'chromium', 
    
    // 日本標準時を設定
    timezoneId: 'Asia/Tokyo',
    locale: 'ja-JP',
    
    // 省略時、失敗時のみスクリーンショットなどを撮る設定が推奨されます
    // screenshot: 'only-on-failure', 
  },
  
  // =======================================================
  // 💡 ここがポイント：webServerプロパティを設定
  // =======================================================
  // webServer: {
  //   // Next.jsの開発サーバーを起動するコマンドを設定
  //   command: 'cd ../operations-prepare && npm run dev', 
    
  //   // サーバーが応答するのを待機するURL
  //   url: 'http://localhost:3000', 
    
  //   // 応答があるまで待機する時間。Next.jsの起動時間に合わせて調整します。
  //   timeout: 60 * 1000, // 例: 60秒間待機
    
  //   // CI環境でなければ基本はfalse（テストごとにサーバーを起動・終了させる）
  //   reuseExistingServer: false, 
  // },
  
});