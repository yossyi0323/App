# minimum-sns-post-app1 共通設定
server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: minimum-sns-post-app1-api
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
